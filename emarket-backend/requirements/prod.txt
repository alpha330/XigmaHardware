# Production runtime dependencies
#
# base.txt already contains Django, Gunicorn, PostgreSQL/Redis clients,
# storage, Sentry and the libraries imported by the application. Add a package
# here only when production settings or code actually use it.
-r base.txt
